import Foundation

/// Set this to the PFI DID that is displayed by the tbdex-pfi-exemplar application
/// when running the `npm run server` command.
let pfiDIDURI = "UPDATE ME!!!"

/// Set this to the string the tbdex-pfi-exemplar outputs when running the `example-issue-credential`
let claim = "UPDATE ME!!!"
